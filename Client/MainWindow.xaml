<Window x:Class="Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client"
        xmlns:viewmodels="clr-namespace:Client.ViewModels"
        xmlns:views="clr-namespace:Client.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="{Binding SystemParameters.PrimaryScreenHeight}" Width="{Binding SystemParameters.PrimaryScreenWidth}">
    <Window.Resources>
        <Style x:Key="RoundedButton" TargetType="{x:Type Button}">
    <Setter Property="HorizontalContentAlignment" Value="Center"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
    <Setter Property="Padding" Value="1"/>
    <Setter Property="Template">
        <Setter.Value>
            <ControlTemplate TargetType="{x:Type Button}">
                <Grid x:Name="grid">
                    <Border x:Name="border" CornerRadius="30" BorderBrush="Black" BorderThickness="1">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          TextElement.FontWeight="Bold">
                        </ContentPresenter>
                    </Border>

                </Grid>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Background" TargetName="border">
                            <Setter.Value>
                                <RadialGradientBrush GradientOrigin="0.496,1.052">
                                    <RadialGradientBrush.RelativeTransform>
                                        <TransformGroup>
                                            <ScaleTransform CenterX="0.5" CenterY="0.5" ScaleX="1.5" ScaleY="1.5"/>
                                            <TranslateTransform X="0.02" Y="0.3"/>
                                        </TransformGroup>
                                    </RadialGradientBrush.RelativeTransform>
                                    <GradientStop Color="#00000000" Offset="1"/>
                                    <GradientStop Color="#FF303031" Offset="0.3"/>
                                </RadialGradientBrush>
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="BorderBrush" TargetName="border" Value="#FF33962B"/>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Opacity" TargetName="grid" Value="0.35"/>
                    </Trigger>

                </ControlTemplate.Triggers>
            </ControlTemplate>
        </Setter.Value>
    </Setter>
</Style>
        
        <DataTemplate x:Name="ScreenShareTemplate" DataType="{x:Type viewmodels:ScreenShareViewModel}">
            <views:ScreenShare DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate x:Name="WhiteboardTemplate" DataType="{x:Type viewmodels:WhiteboardViewModel}">
            <views:Whiteboard DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate x:Name="ChatTemplate" DataType="{x:Type viewmodels:ChatViewModel}">
            <views:Chat DataContext="{Binding }"/>
        </DataTemplate>
        <DataTemplate x:Name="DashboardTemplate" DataType="{x:Type viewmodels:DashboardViewModel}">
            <views:Dashboard DataContext="{Binding}"/>
        </DataTemplate>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="0.01*"/>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width="0.01*"/>
            <ColumnDefinition Width="1.5*"/>
        </Grid.ColumnDefinitions>
       
        <Grid.RowDefinitions>
           <RowDefinition Height="0.95*"/>
           <RowDefinition Height="0.075*"/>
           <RowDefinition Height="10*"/>
           <RowDefinition Height="0.75"/>
           <RowDefinition Height="1.2*"/>
           <RowDefinition Height="0.085*"/>
           <RowDefinition Height="0.015*"/>
       </Grid.RowDefinitions>
    
        <DockPanel Background="Aqua" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="5"/>
        
        <DockPanel Name="UsersListPane" Background="Wheat" Grid.Row="2" Grid.Column="0" Grid.RowSpan="4" Visibility="Visible"/>
        <Button Grid.Row="4" Grid.Column="0" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" Width="Auto" Height="32" FontSize="16" VerticalAlignment="Bottom" HorizontalAlignment="Center" Background="White" Click="Users_List_Clicked">Users List</Button>

        <DockPanel Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="3" Width="300">
            <Button Content="Whiteboard" Width="Auto" Margin="2.5 0" Click="Whiteboard_Clicked" Style="{DynamicResource RoundedButton}"/>
            <Button Content="ScreenShare" Width="Auto" Margin="2.5 0" Click="ScreenShare_Clicked" Style="{DynamicResource RoundedButton}"/>
           <!-- <Button Content="Chat" Width="Auto" Margin="2.5 0"/> -->
            <Button Content="Dashboard" Width="Auto" Margin="2.5 0" Click="Dashboard_Clicked" Style="{DynamicResource RoundedButton}"/>
            <Button Content="Leave Room" Width="Auto" Margin="2.5 0" Style="{DynamicResource RoundedButton}"/>
            
        </DockPanel>
        
        <DockPanel Name="ChatPane" Background="Orange" Grid.Row="2" Grid.Column="4" Grid.RowSpan="4" Visibility="Collapsed"/>
        <Button Grid.Row="4" Grid.Column="4" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" Width="Auto" Height="32" FontSize="16" VerticalAlignment="Bottom" HorizontalAlignment="Center" Background="White" Click="Chat_Button_Clicked">Chat</Button>
 
        
        <ContentControl Name="SSwb" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="5" Grid.RowSpan="1" Content="{Binding}"/> 
       <!-- <ContentControl Name="ChatPane" Grid.Row="2" Grid.Column="4" Grid.RowSpan="4" Content="{Binding }"/> -->

    </Grid>
</Window>
